spring:
  datasource:
    driver-Class-Name: org.postgresql.Driver

  jpa:
    properties:
      hibernate:
        format_sql: true
        dialect: org.hibernate.dialect.PostgreSQL81
    hibernate.ddl-auto: create-drop
    generate-ddl: true
    show-sql: true